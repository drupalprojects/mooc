// JavaScript Document
(function ($) {
  $(document).ready(function(){
    // TODO: make this bound to top control bar once its built
    /*$('#page').click(function(){
      if ($('#thumbnav_controller_right').hasClass('thumbnav_controller_right')) {
        Drupal.thumbnav.leftControllerHide();
        Drupal.thumbnav.rightControllerHide();
      }
      else {
        Drupal.thumbnav.leftControllerShow();
        Drupal.thumbnav.rightControllerShow();
      }
    });*/
  });
})(jQuery);